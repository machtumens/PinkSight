
features_path: data/processed/semiquant_features.npz
embeddings_dir: reports/G2_imaging/embeddings
clin_embed_dir: reports/G2_imaging/embeddings
manifest: data/manifest_v1.csv
split_yaml: configs/split_v2.yaml

seeds: [0, 1, 2]
n_splits: 5

encoder_arch:
  hidden: 64
  out: 512

probe_C: 1.0

kill_gate:
  delta_auc_min: 0.0
  delong_lb_min: 0.0
  n_bootstrap: 1000
  shuffle_tol: 0.03

smoke:
  n_patients: 100
  n_splits: 2
  seeds: [0]
